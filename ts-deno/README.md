# deno-benchmark

To install dependencies:

```bash
deno install
```

To run:

```bash
deno run main.ts
```

This project was created using `deno init` in deno v1.3.4. [Deno](https://deno.com) is a fast all-in-one JavaScript runtime.
